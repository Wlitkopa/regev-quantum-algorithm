"""Internal helpers that back the :class:`implementations.regev.Regev` facade.

The facade in ``regev.py`` remains the entry point used by ``regev_all.py``;
these submodules only carry stateless, well-scoped helpers so that the main
class stays readable.
"""
